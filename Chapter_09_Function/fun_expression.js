const msg = function(name) {
    return "Hello, " + name + "!";  
};
console.log(msg("Alice")); // Output: Hello, Alice!

const sum = function(a, b) {
    return a + b;  
};
console.log(sum(5, 3)); // Output: 8
